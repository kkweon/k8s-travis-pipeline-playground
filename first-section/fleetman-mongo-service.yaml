apiVersion: v1
kind: Service
metadata:
  labels:
    app: fleetman-mongo
  name: fleetman-mongo
spec:
  ports:
  - name: "27017"
    port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: fleetman-mongo
  type: ClusterIP
